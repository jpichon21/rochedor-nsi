{% extends 'base-admin.html.twig' %}

{% block body %}
    <h1>Shipping creation</h1>
    {% form_theme form 'jquery.collection.html.twig' %}
     
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('admin_shipping_index') }}">Back to the list</a>
        </li>
    </ul>

{% endblock %}
