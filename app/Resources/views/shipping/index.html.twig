{% extends 'base-admin.html.twig' %}

{% block body %}
    <h1>Packagings list</h1>
    <div class="table-responsive">
        <table class="table">
            <thead>
                <tr>
                    <th scope="col">Id</th>
                    <th scope="col">Name</th>
                    <th scope="col">Weight</th>
                    <th scope="col">Country</th>
                    <th scope="col">Price</th>
                    <th scope="col">Related Countrys</th>
                    <th scope="col">Maximal Weight</th>
                    <th scope="col">Actions</th>
                </tr>
            </thead>
            <tbody>
            {% for shipping in shippings %}
                <tr>
                    <td><a href="{{ path('admin_shipping_show', { 'id': shipping.id }) }}">{{ shipping.id }}</a></td>
                    <td>{{ shipping.name }}</td>
                    <td>{{ shipping.weight }}</td>
                    <td>{{ shipping.country }}</td>
                    <td>{{ shipping.price }}</td>
                    <td>{% if shipping.relatedCountrys %}{{ shipping.relatedCountrys|join(', ') }}{% endif %}</td>
                    <td>{{ shipping.maximalWeight }}</td>
                    <td>
                        <ul>
                            <li>
                                <a href="{{ path('admin_shipping_show', { 'id': shipping.id }) }}">show</a>
                            </li>
                            <li>
                                <a href="{{ path('admin_shipping_edit', { 'id': shipping.id }) }}">edit</a>
                            </li>
                        </ul>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
    <ul>
        <li>
            <a href="{{ path('admin_shipping_new') }}">Create a new Packaging</a>
        </li>
    </ul>
{% endblock %}
