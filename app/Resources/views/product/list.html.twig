<html>
  <body>
    Page: 
    {{ dump(page) }}
    Locale: 
    {{ dump(avaiableLocales) }}
    Series: 
    {{ dump(series) }}
    Themes:
    {{ dump(themes) }}
    Products categorized:
    {{ dump(productsCategorized) }}
    Products
    {{ dump(products) }}
  </body>
</html>